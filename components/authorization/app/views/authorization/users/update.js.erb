toastr.success('User data is updated successfully');
$('#editUser').modal('hide');
/* manually push the URL to previous state. This will go two step back in history and make an Ajax call to restore the groups index page */
history.go(-2);
